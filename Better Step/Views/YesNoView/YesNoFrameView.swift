//
//  YesNoFrameView.swift
//  Better Step
//
//  Created by Fritz Anderson on 12/31/21.
//

import SwiftUI
/*

struct YesNoFrameView: View {
    let boundingSize: CGSize
    let count: Int

    func outerSize() -> CGSize {
        let buttonSize = YesNoButton.buttonSize(within: boundingSize)
        return CGSize(
            width: buttonSize.width,
            height: CGFloat(count) * buttonSize.height)
    }

    var body: some View {
        RoundedRectangle(cornerRadius: 8)
            .stroke(Color.black, lineWidth: 1)
            .frame(
                width : outerSize().width,
                height: outerSize().height)
            .foregroundColor(.white)
            .padding(1.0)
    }
}

struct YesNoFrameView_Previews: PreviewProvider {
    static var previews: some View {
        YesNoFrameView(boundingSize: CGSize(width: 320, height: 240),
                       count: 2)
    }
}
*/
